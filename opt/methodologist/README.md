With default `application-dev.properties` configuration, a `methodologist-build.jar` is expected to be placed here manually.
To generate it, refer to the top-level README.md.

This file ensures that the folder is versioned in git, do not delete it.